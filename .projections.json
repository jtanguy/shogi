{
    "shogi.cabal": { "command": "cabal" },
    "src/*.hs": {"template": ["module {capitalize|dot} where"]},
    "lib/*.hs": {"template": ["module {capitalize|dot} where"]},
    "test/*/Tests.hs": { "template": ["module {capitalize|dot}.Tests where"]},
    "*": {"make": "cabal build"}
}
